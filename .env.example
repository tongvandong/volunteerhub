# Copy to .env and edit before running `docker compose up`.
# (.env is git-ignored / docker-ignored — never commit real secrets.)

# --- SQL Server SA password ---------------------------------------------
# Must satisfy SQL Server complexity: >= 8 chars, upper + lower + digit + symbol.
SA_PASSWORD=Ch4ngeMe_Str0ng!

# --- JWT signing key ------------------------------------------------------
# Long random string. All services MUST share the same value to validate tokens.
# Generate e.g.: openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-secret-at-least-32-chars

# --- Redis password -------------------------------------------------------
REDIS_PASSWORD=Ch4ngeMe_Redis!

# --- Neo4j password (knowledge graph) -------------------------------------
# Đổi mật khẩu này thì phải xóa volume neo4j_data (mật khẩu chỉ đặt lúc khởi tạo).
NEO4J_PASSWORD=Ch4ngeMe_Neo4j!

# --- Public URL of the site ----------------------------------------------
# Used for CORS and for the verify link encoded into certificate QR codes.
# Local test:   http://localhost
# Production:   https://volunteerhub.example.com
PUBLIC_URL=http://localhost

# --- SMTP email for forgot-password --------------------------------------
# Gmail example:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_ENABLE_SSL=true
# SMTP_USERNAME=your-gmail@gmail.com
# SMTP_PASSWORD=your-16-character-app-password-without-spaces
# SMTP_FROM_EMAIL=your-gmail@gmail.com
# SMTP_FROM_NAME=VolunteerHub
SMTP_HOST=
SMTP_PORT=587
SMTP_ENABLE_SSL=true
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=VolunteerHub

# --- Daily video interview ----------------------------------------------
# Put real values in the VPS .env. Never commit the real Daily API key.
DAILY_API_KEY=
DAILY_DOMAIN=
